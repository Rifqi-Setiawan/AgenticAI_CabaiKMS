# Final research validation campaign

This directory is readiness-only. The repository currently contains one eligible real workbook with 13 annotatable attributes, so no final master, annotation templates, or gold are generated here.

The researcher must supply additional independent, non-synthetic field workbooks covering heterogeneous structures and both supported evaluation orientations. The project methodology must also preregister a defensible minimum annotatable-item count. Re-run the readiness command after updating the manifest with the real files and hashes.
